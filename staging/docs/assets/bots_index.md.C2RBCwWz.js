import{_ as t,o as s,c as a,ag as o}from"./chunks/framework.DMVuHR7L.js";const u=JSON.parse('{"title":"For agents","description":"","frontmatter":{},"headers":[],"relativePath":"bots/index.md","filePath":"bots/index.md","lastUpdated":1778756371000}'),i={name:"bots/index.md"};function n(r,e,d,c,l,h){return s(),a("div",null,[...e[0]||(e[0]=[o(`<h1 id="for-agents" tabindex="-1">For agents <a class="header-anchor" href="#for-agents" aria-label="Permalink to &quot;For agents&quot;">​</a></h1><p>This section is the operational contract for AI agents working on WebLogcat. Pages are intentionally terse — the goal is <em>agent succeeds at the task</em>, not <em>human enjoys the read</em>.</p><p>If you are a human contributor, <a href="./../devs/">devs/</a> is the friendlier mirror of most of this material. The rules here apply to you too.</p><h2 id="read-these-first" tabindex="-1">Read these first <a class="header-anchor" href="#read-these-first" aria-label="Permalink to &quot;Read these first&quot;">​</a></h2><ul><li><a href="./widgets-contract">Widget contract</a> — what a new widget must satisfy to slot into the dashboard cleanly. Authoritative; the dashboard shared chrome assumes everything in this file.</li><li><a href="./doc-sync">Doc-sync rules</a> — when and how to update the <code>docs/features/</code> pages alongside a code change. PRs that ship UI changes without a docs delta will be bounced.</li><li><a href="./test-sync">Test-sync rules</a> — what new behaviour must be covered by <code>vitest</code> (unit) and <code>@playwright/test</code> (e2e), and how to keep the existing simulator-driven coverage current.</li><li><a href="./screenshots">Screenshot pipeline</a> — how to refresh the docs and README screenshots on demand. Use this when a UI change affects imagery or when the user asks for a fresh shot.</li><li><a href="./maintaining">Maintaining <code>bots/</code></a> — how to fold your own learnings back into this directory without making it worse. Read before editing anything under <code>bots/</code>.</li></ul><h2 id="working-principles-do-these-every-time" tabindex="-1">Working principles (do these every time) <a class="header-anchor" href="#working-principles-do-these-every-time" aria-label="Permalink to &quot;Working principles (do these every time)&quot;">​</a></h2><ol><li><strong>Start with <code>CLAUDE.md</code> in the repo root.</strong> It is the implicit prompt for this codebase and lists hard constraints (TS strict, no new runtime deps, zero-cost infra, history is read-only, <code>oklch()</code> for colours).</li><li><strong>Use the shared chrome.</strong> Don&#39;t invent new patterns for things the dashboard already provides — toasts (<code>useDashboardChrome</code>), log stream (<code>useLogStream</code>), per-widget settings modal, eye-toggle bar modes, etc. Reach for the existing primitives first; if you can&#39;t make them work, ask before forking.</li><li><strong>Cover behaviour with tests in the same PR.</strong> New widget → unit tests for any pure logic, e2e against the simulator. Existing widget changes → update the e2e selectors / assertions if the UI moved.</li><li><strong>Sync the user docs.</strong> New flow → new section in the matching <code>docs/features/&lt;kind&gt;.md</code>. Renamed UI control → grep <code>docs/features/</code> and update. See <a href="./doc-sync">doc-sync</a> for the full contract.</li><li><strong>Surface ambiguity early.</strong> If the task spec is ambiguous, ask the user a short, specific question rather than guessing. Cleanup is more expensive than clarification.</li></ol><h2 id="where-things-live" tabindex="-1">Where things live <a class="header-anchor" href="#where-things-live" aria-label="Permalink to &quot;Where things live&quot;">​</a></h2><ul><li>Production code: <code>src/</code> — see <a href="./../devs/architecture">devs/architecture</a>.</li><li>Build / deploy: <code>vite.config.ts</code>, <code>.github/workflows/</code>, <a href="./../devs/deployment">devs/deployment</a>.</li><li>Issue templates: <code>.github/ISSUE_TEMPLATE/</code>.</li><li>Docs: <code>docs/</code> (this site).</li><li>Tests: <code>tests/smoke.spec.ts</code> (Playwright e2e) and <code>src/**/*.test.ts</code> (Vitest unit).</li></ul><h2 id="what-done-looks-like" tabindex="-1">What &quot;done&quot; looks like <a class="header-anchor" href="#what-done-looks-like" aria-label="Permalink to &quot;What &quot;done&quot; looks like&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>npm run typecheck     # strict TS</span></span>
<span class="line"><span>npm run lint          # eslint --max-warnings 0</span></span>
<span class="line"><span>npm test              # vitest run</span></span>
<span class="line"><span>npm run build         # vite build</span></span>
<span class="line"><span>npm run e2e           # playwright (simulator-driven)</span></span>
<span class="line"><span>npm run docs:build    # vitepress build</span></span></code></pre></div><p>All green. Then commit, push to the branch the parent task assigned, let CI gate the merge. Don&#39;t push to <code>release</code> directly.</p>`,12)])])}const g=t(i,[["render",n]]);export{u as __pageData,g as default};
