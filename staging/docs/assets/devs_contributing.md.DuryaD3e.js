import{_ as s,o as i,c as t,ag as a}from"./chunks/framework.DMVuHR7L.js";const u=JSON.parse('{"title":"Contributing guide","description":"","frontmatter":{},"headers":[],"relativePath":"devs/contributing.md","filePath":"devs/contributing.md","lastUpdated":1778207311000}'),o={name:"devs/contributing.md"};function n(l,e,r,d,c,h){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="contributing-guide" tabindex="-1">Contributing guide <a class="header-anchor" href="#contributing-guide" aria-label="Permalink to &quot;Contributing guide&quot;">​</a></h1><p>This is a small repo with a clear scope, so the rules are short.</p><h2 id="branches" tabindex="-1">Branches <a class="header-anchor" href="#branches" aria-label="Permalink to &quot;Branches&quot;">​</a></h2><ul><li><code>main</code> — integration branch. Every push deploys to <strong>staging</strong>: <code>https://&lt;owner&gt;.github.io/web-logcat/staging/</code>.</li><li><code>release</code> — production branch. Pushes (typically fast-forward merges from <code>main</code> after smoke-testing on staging) deploy to: <code>https://&lt;owner&gt;.github.io/web-logcat/</code>.</li><li>Feature work happens on <code>claude/&lt;topic&gt;</code> or <code>feature/&lt;topic&gt;</code> branches and lands via PR into <code>main</code>.</li></ul><h2 id="pull-requests" tabindex="-1">Pull requests <a class="header-anchor" href="#pull-requests" aria-label="Permalink to &quot;Pull requests&quot;">​</a></h2><ul><li>CI must pass (<code>npm run typecheck</code>, <code>npm run lint --max-warnings 0</code>, <code>npm test</code>, <code>npm run build</code>, <code>npm run e2e</code>).</li><li>Keep PRs small and focused. The component scaffolding in <code>src/components/</code> is intentionally split so each component can be ported in its own PR.</li><li>New or changed user-facing behaviour comes with a docs update — see <a href="./docs-conventions">docs conventions</a>. Reviewers will push back if the <a href="./../features/">features pages</a> drift from the shipped UI.</li><li>Core flows are exercised by tests — see <a href="./../bots/test-sync">test-sync rules</a>. New widgets / behaviours introduce or extend their corresponding unit + e2e coverage in the same PR.</li></ul><h2 id="promoting-staging-→-production" tabindex="-1">Promoting staging → production <a class="header-anchor" href="#promoting-staging-→-production" aria-label="Permalink to &quot;Promoting staging → production&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> checkout</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> release</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> merge</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --ff-only</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> main</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> push</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> origin</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> release</span></span></code></pre></div><p>The <code>release</code> push triggers <code>.github/workflows/deploy.yml</code> and updates the production URL while preserving <code>/staging/</code>. The same workflow publishes the docs at <code>&lt;base&gt;/docs/</code>.</p><h2 id="local-development" tabindex="-1">Local development <a class="header-anchor" href="#local-development" aria-label="Permalink to &quot;Local development&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">          # main app on http://localhost:5173</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docs:dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # docs site on http://localhost:5174</span></span></code></pre></div><p>Notes:</p><ul><li>WebUSB requires HTTPS or localhost. <code>npm run dev</code> binds to localhost so it works without certs, but the real ADB code path can only be exercised on the deployed Pages site (or with a local HTTPS dev cert).</li><li>The simulated stream is enabled via the <strong>Use simulated data</strong> button on the empty state.</li><li>The docs are a separate VitePress app — building or running them is fully decoupled from the main Vite app, so you can edit Markdown without restarting the dashboard dev server.</li></ul><h2 id="code-style" tabindex="-1">Code style <a class="header-anchor" href="#code-style" aria-label="Permalink to &quot;Code style&quot;">​</a></h2><ul><li>TypeScript strict mode.</li><li>Functional components, hooks. State lives in <code>App.tsx</code> until a clear reason to extract it appears (no premature contexts / stores).</li><li>CSS custom properties only — don&#39;t introduce CSS-in-JS or a utility framework. The token system in <code>src/styles/tokens.css</code> is the contract.</li><li><code>oklch()</code> for new colors so they stay perceptually consistent across themes; do not hardcode hex.</li></ul>`,15)])])}const g=s(o,[["render",n]]);export{u as __pageData,g as default};
