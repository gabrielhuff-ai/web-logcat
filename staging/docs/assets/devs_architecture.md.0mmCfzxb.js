import{_ as s,o as a,c as n,ag as t}from"./chunks/framework.DMVuHR7L.js";const g=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"devs/architecture.md","filePath":"devs/architecture.md","lastUpdated":1778606729000}'),o={name:"devs/architecture.md"};function p(i,e,l,d,c,r){return a(),n("div",null,[...e[0]||(e[0]=[t(`<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><h2 id="module-map-v2" tabindex="-1">Module map (v2) <a class="header-anchor" href="#module-map-v2" aria-label="Permalink to &quot;Module map (v2)&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/</span></span>
<span class="line"><span>├── main.tsx                      # entry: mounts &lt;App/&gt;, imports CSS</span></span>
<span class="line"><span>├── types.ts                      # LogEntry, Filter, Tweaks, DeviceInfo,</span></span>
<span class="line"><span>│                                 # WidgetKind, Tile, LayoutState</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>├── lib/</span></span>
<span class="line"><span>│   ├── filters.ts                # parse/match/highlight — pure</span></span>
<span class="line"><span>│   ├── logGenerator.ts           # simulator (lazy-loaded)</span></span>
<span class="line"><span>│   ├── tweaks.ts                 # useTweaks() — localStorage prefs</span></span>
<span class="line"><span>│   ├── adb.ts                    # WebUSB + ADB transport (lazy)</span></span>
<span class="line"><span>│   ├── adbContext.ts             # AdbContext + useAdb() hook</span></span>
<span class="line"><span>│   ├── AdbProvider.tsx           # &lt;AdbProvider/&gt; — wraps the dashboard</span></span>
<span class="line"><span>│   ├── logStream.ts              # LogStreamHub — single-upstream pub/sub</span></span>
<span class="line"><span>│   ├── logStreamContext.ts       # context + useLogStream()</span></span>
<span class="line"><span>│   ├── dashboardChrome.ts        # tweaks + showToast for widgets</span></span>
<span class="line"><span>│   ├── widgets.ts                # widget registry (kind → def)</span></span>
<span class="line"><span>│   ├── layout.ts                 # dwindle binary-tree layout + persistence</span></span>
<span class="line"><span>│   ├── shellSim.ts               # in-memory ADB-shell built-ins (sim path)</span></span>
<span class="line"><span>│   ├── format.ts                 # formatTs, rowHeightFor</span></span>
<span class="line"><span>│   └── knownNames.ts             # static tag/process pools for autocomplete</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>├── components/</span></span>
<span class="line"><span>│   ├── App.tsx                   # owns device + LogStreamHub; renders</span></span>
<span class="line"><span>│   │                             # &lt;EmptyState/&gt; or &lt;Dashboard/&gt;</span></span>
<span class="line"><span>│   ├── EmptyState.tsx            # pre-connection screen</span></span>
<span class="line"><span>│   ├── Dashboard.tsx             # connected shell + DashTopbar</span></span>
<span class="line"><span>│   ├── TileGrid.tsx              # dwindle tree renderer; split-handle resize, swap-drag</span></span>
<span class="line"><span>│   ├── Tile.tsx                  # tile chrome (header + body)</span></span>
<span class="line"><span>│   ├── WidgetPalette.tsx         # +Add widget modal</span></span>
<span class="line"><span>│   ├── FilterBar.tsx             # chip input + transport</span></span>
<span class="line"><span>│   ├── LevelRow.tsx              # V/D/I/W/E pills + rate</span></span>
<span class="line"><span>│   ├── LogList.tsx               # virtualised log area + sticky pinned block</span></span>
<span class="line"><span>│   ├── LogRow.tsx                # one log line, with highlight rendering</span></span>
<span class="line"><span>│   ├── Heatmap.tsx               # 60-cell gutter</span></span>
<span class="line"><span>│   ├── HelpDialog.tsx            # ? shortcuts dialog</span></span>
<span class="line"><span>│   ├── SearchOverlay.tsx         # ⌘F floating search box</span></span>
<span class="line"><span>│   ├── Icons.tsx                 # inline SVG icon set</span></span>
<span class="line"><span>│   └── widgets/</span></span>
<span class="line"><span>│       ├── LogcatWidget.tsx      # the v1 logcat experience as a widget</span></span>
<span class="line"><span>│       ├── ShellWidget.tsx       # interactive ADB shell (one channel per instance)</span></span>
<span class="line"><span>│       ├── DumpsysWidget.tsx     # preset dumpsys runner + parsed cards / raw view</span></span>
<span class="line"><span>│       ├── FilesWidget.tsx       # tree + list browser over adb.sync()</span></span>
<span class="line"><span>│       └── MirrorWidget.tsx      # scrcpy-style mirror with WebCodecs decode</span></span>
<span class="line"><span>│</span></span>
<span class="line"><span>└── styles/</span></span>
<span class="line"><span>    ├── tokens.css                # design original — colors, spacing, motion</span></span>
<span class="line"><span>    │                             # (extended with v2 --shadow-3 + --glass-line)</span></span>
<span class="line"><span>    ├── app.css                   # design original — layout + log row + panels</span></span>
<span class="line"><span>    ├── dashboard.css             # design original (v2) — tile grid + chrome</span></span>
<span class="line"><span>    ├── components.css            # cross-cutting additions (empty state, filter</span></span>
<span class="line"><span>    │                             # bar, level pills, heatmap, help dialog,</span></span>
<span class="line"><span>    │                             # palette, tile loading)</span></span>
<span class="line"><span>    └── widgets/                  # per-widget design CSS (Phase 10 split);</span></span>
<span class="line"><span>        ├── logcat.css            # imported by the widget itself so the</span></span>
<span class="line"><span>        ├── shell.css             # rules co-load with the lazy chunk</span></span>
<span class="line"><span>        ├── dumpsys.css</span></span>
<span class="line"><span>        ├── files.css</span></span>
<span class="line"><span>        └── mirror.css</span></span></code></pre></div><p><code>tokens.css</code>, <code>app.css</code>, and <code>dashboard.css</code> are <strong>design originals</strong> — copied verbatim from <code>design/v1/source/styles.css</code> (tokens + app) and <code>design/v2/source/dashboard.css</code>. The v1 → v2 design diff added two tokens (<code>--shadow-3</code> + <code>--glass-line</code>); they&#39;re added inline at the top of each theme block in <code>tokens.css</code>. Everything else stays untouched so those files can be refreshed from the design source without merge conflicts. Per-widget CSS lives under <code>styles/widgets/&lt;kind&gt;.css</code> and is imported at the top of the matching widget component. Cross-cutting additions (palette, tile loading, help dialog, heatmap, etc.) stay in <code>components.css</code>.</p><h2 id="state" tabindex="-1">State <a class="header-anchor" href="#state" aria-label="Permalink to &quot;State&quot;">​</a></h2><p>Top-level state lives in <code>&lt;App/&gt;</code>:</p><table tabindex="0"><thead><tr><th>State</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>device</code></td><td><code>DeviceInfo | null</code></td><td><code>null</code> ⇒ render <code>&lt;EmptyState/&gt;</code></td></tr><tr><td><code>usingFake</code></td><td><code>boolean</code></td><td>true when streaming the simulator</td></tr><tr><td><code>adb</code></td><td><code>Adb | null</code></td><td>live ADB handle (Phase 6+)</td></tr><tr><td><code>tweaks</code></td><td><code>Tweaks</code> (via <code>useTweaks</code>)</td><td>persisted prefs</td></tr><tr><td><code>toast</code></td><td><code>string | null</code></td><td>bottom-centre acknowledgement</td></tr><tr><td><code>helpOpen</code></td><td><code>boolean</code></td><td><code>?</code> shortcuts dialog</td></tr></tbody></table><p>Widget state — filters, paused, autoScroll, levelEnabled, pinned, expanded — moved from <code>&lt;App/&gt;</code> to <code>&lt;LogcatWidget/&gt;</code>. Two Logcat tiles on the same device get independent chip bars (persisted under <code>weblogcat:filters:&lt;serial&gt;:&lt;tileId&gt;</code>).</p><p>Tile-grid state lives in <code>&lt;TileGrid/&gt;</code>: the layout tree, the maximized tile id, and the in-flight resize / swap-drag state. Persisted to <code>localStorage</code> under <code>weblogcat-dashboard-v2</code>. The tree is a binary &quot;dwindle&quot; layout (Hyprland-style) — each split node carves its parent area along one axis at a configurable ratio; leaves host tiles. Adding a tile splits the focused leaf in two; removing collapses the parent split into the surviving sibling. Resizing happens at the seam between two siblings (<code>.dash-split-handle</code>); rearrangement is drag-to-swap on tile headers. The dashboard is a fixed-size, non-scrolling viewport — tiles share every pixel of available space.</p><h2 id="the-stream" tabindex="-1">The stream <a class="header-anchor" href="#the-stream" aria-label="Permalink to &quot;The stream&quot;">​</a></h2><p><code>&lt;App/&gt;</code> owns a single <code>LogStreamHub</code> — see <code>lib/logStream.ts</code>. The transport (real ADB or simulator) calls <code>hub.publishMany(entries)</code> once per ingest tick; the hub keeps a ring buffer (cap <code>MAX_LOGS</code> = 5000) and fans out to N <code>LogcatWidget</code> subscribers. New subscribers get a snapshot on mount.</p><p>Why a hub: K Logcat tiles previously meant K shell channels and K × 50k row buffers. With the hub there&#39;s one upstream and one buffer; per- widget memory is just whatever the widget keeps for its filter view.</p><h2 id="performance-notes" tabindex="-1">Performance notes <a class="header-anchor" href="#performance-notes" aria-label="Permalink to &quot;Performance notes&quot;">​</a></h2><ul><li><code>&lt;LogRow/&gt;</code> is wrapped in <code>memo</code>; <code>&lt;LogList/&gt;</code> virtualises past 800 visible rows via <code>@tanstack/react-virtual</code>.</li><li>The resize / swap-drag loop in <code>&lt;TileGrid/&gt;</code> coalesces pointer-move events into the next animation frame — protects against &gt;120Hz pointer devices flooding React with state updates.</li></ul><h2 id="theming" tabindex="-1">Theming <a class="header-anchor" href="#theming" aria-label="Permalink to &quot;Theming&quot;">​</a></h2><p><code>tokens.css</code> defines the entire palette as CSS custom properties keyed off <code>--accent-hue</code> (set by <code>data-accent</code>) and theme (<code>data-theme</code>). The <code>useTweaks</code> hook applies both attributes to <code>&lt;html&gt;</code> so changes propagate without any JS-side recompute.</p>`,16)])])}const u=s(o,[["render",p]]);export{g as __pageData,u as default};
